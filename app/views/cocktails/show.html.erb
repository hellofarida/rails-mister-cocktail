<h1><%= @cocktail.name %></h1>
<strong>Ingredients:</strong>
<ul>
  <% @cocktail.doses.each do |dose| %>
    <li> <%= dose.description %> <%= dose.ingredient.name %> - <%= link_to "delet", dose_path(dose),
      method: :delete,
      data: { confirm: "Are you sure you want to delete: #{dose.ingredient.name}?" } %></li>
  <% end %>
</ul>
<%= link_to 'Add a dose', new_cocktail_dose_path(@cocktail) %>
<hr>
<%= link_to 'Back to all cocktails', cocktails_path %>
